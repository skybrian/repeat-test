{
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0",
    "@std/testing": "jsr:@std/testing@^1.0.0",
    "pure-rand": "npm:pure-rand@^6.1.0"
  },
  "tasks": {
    "coverage": "rm -f lcov.info && deno test --coverage --clean && deno coverage --lcov > lcov.info && deno coverage",
    "test": "deno test"
  }
}
